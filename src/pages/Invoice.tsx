import { Paper } from '@material-ui/core';

const Invoice = () => {
    return (
        <Paper square style={{ minHeight: 'calc(100vh - 80px)' }}>
            Invoice
        </Paper>
    );
};

export default Invoice;
